steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/remember-rem-job:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/remember-rem-job:latest'
      - '.'

  # Push to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/remember-rem-job:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/remember-rem-job:latest'

  # Deploy as Cloud Run Job
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'deploy'
      - 'remember-rem-job'
      - '--image=gcr.io/$PROJECT_ID/remember-rem-job:$COMMIT_SHA'
      - '--region=us-central1'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--task-timeout=3000s'
      - '--max-retries=0'
      - '--set-env-vars=NODE_ENV=production'
      - '--update-secrets=WEAVIATE_REST_URL=remember-weaviate-rest-url:latest,WEAVIATE_GRPC_URL=remember-weaviate-grpc-url:latest,WEAVIATE_API_KEY=remember-weaviate-api-key:latest,FIREBASE_PROJECT_ID=remember-firebase-project-id:latest,FIREBASE_ADMIN_SERVICE_ACCOUNT_KEY=remember-firebase-admin-service-account-key:latest,EMBEDDINGS_PROVIDER=remember-embeddings-provider:latest,EMBEDDINGS_MODEL=remember-embeddings-model:latest,EMBEDDINGS_API_KEY=remember-openai-embeddings-api-key:latest,ANTHROPIC_API_KEY=remember-anthropic-api-key:latest'

images:
  - 'gcr.io/$PROJECT_ID/remember-rem-job:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/remember-rem-job:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
